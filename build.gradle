allprojects {
	apply plugin: 'eclipse'
	apply plugin: 'idea'
	
	repositories {
		mavenCentral()
		maven {
			url "http://clojars.org/repo/"
		}
	}
}

subprojects {
	apply plugin: 'java'
	tasks.withType(JavaCompile) { options.encoding = "UTF-8" }
}